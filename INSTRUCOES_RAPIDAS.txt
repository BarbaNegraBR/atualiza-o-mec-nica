═══════════════════════════════════════════════════════════════
  GUIA RÁPIDO - SISTEMA DE ATUALIZAÇÃO AUTOMÁTICA
═══════════════════════════════════════════════════════════════

📋 CONFIGURAÇÃO INICIAL (APENAS UMA VEZ)
───────────────────────────────────────────────────────────────

1. Configure a sincronização automática:
   
   GitHub Desktop:
   - Abra GitHub Desktop
   - File → Add Local Repository
   - Selecione esta pasta
   - Remote: https://github.com/BarbaNegraBR/atualiza-o-mec-nica.git
   
   OU via terminal:
   git init
   git remote add origin https://github.com/BarbaNegraBR/atualiza-o-mec-nica.git

2. Instale as dependências:
   pip install -r requirements.txt


🚀 COMO PUBLICAR UMA NOVA VERSÃO
───────────────────────────────────────────────────────────────

1. Compile o executável:
   build_executavel.bat

2. Atualize o versao.json com a nova versão:
   Edite versao.json e incremente a versão (ex: 1.0.0 → 1.0.1)

3. Use GitHub Desktop ou extensão do editor:
   - Faça commit das alterações
   - Faça push para o GitHub

4. Crie uma Release no GitHub:
   - Acesse: https://github.com/BarbaNegraBR/atualiza-o-mec-nica/releases/new
   - Tag: v1.0.1 (mesma versão do versao.json)
   - Faça upload: dist/Calculadora_Reparos_Palomino.exe
   - Nome na release: Calculadora_Reparos_Palomino.exe


✅ COMO FUNCIONA PARA OS USUÁRIOS
───────────────────────────────────────────────────────────────

- Ao abrir o aplicativo, ele verifica automaticamente se há atualização
- Se houver, mostra uma mensagem perguntando se deseja atualizar
- Se o usuário aceitar, baixa e instala automaticamente
- O aplicativo é reiniciado com a nova versão


⚠️ IMPORTANTE
───────────────────────────────────────────────────────────────

- O arquivo versao.json deve estar na branch main/master
- As releases devem ter tags no formato: v1.0.0, v1.0.1, etc.
- O nome do arquivo na release deve ser: Calculadora_Reparos_Palomino.exe
- A URL do versao.json deve estar acessível publicamente


🔧 SOLUÇÃO DE PROBLEMAS
───────────────────────────────────────────────────────────────

Erro: "Git não está instalado"
→ Instale: https://git-scm.com/downloads

Erro: "Executável não encontrado"
→ Execute primeiro: build_executavel.bat

Erro ao fazer push
→ Configure suas credenciais Git:
  git config --global user.name "Seu Nome"
  git config --global user.email "seu@email.com"

Atualização não aparece para usuários
→ Verifique se o versao.json está no GitHub na branch correta
→ Verifique se a release foi criada corretamente
→ Verifique se o nome do arquivo na release está correto

